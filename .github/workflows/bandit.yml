name: Bandit Scan

on:
  push:
  pull_request:

jobs:
  bandit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run Bandit
        uses: jpetrucciani/bandit-check@v1
        with:
          path: "."
